# collision

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build collision` to build the library.

## Running unit tests

Run `nx test collision` to execute the unit tests via [Jest](https://jestjs.io).
