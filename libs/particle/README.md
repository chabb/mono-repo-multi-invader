# particle

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build particle` to build the library.

## Running unit tests

Run `nx test particle` to execute the unit tests via [Jest](https://jestjs.io).
