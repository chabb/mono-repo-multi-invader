# music

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build music` to build the library.

## Running unit tests

Run `nx test music` to execute the unit tests via [Jest](https://jestjs.io).
